// This file is auto-generated by @hey-api/openapi-ts

import {
  type DefaultError,
  type InfiniteData,
  infiniteQueryOptions,
  queryOptions,
} from '@tanstack/react-query';

import { client } from '../client.gen.js';
import {
  getAsset,
  getContract,
  getInterface,
  getLedger,
  getNetworkStats,
  getTransaction,
  health,
  listAssets,
  listAssetTransactions,
  listEvents,
  listInvocations,
  listLedgers,
  listParticipants,
  listTransactions,
  type Options,
} from '../sdk.gen.js';
import type {
  GetAssetData,
  GetAssetError,
  GetAssetResponse,
  GetContractData,
  GetContractError,
  GetContractResponse,
  GetInterfaceData,
  GetInterfaceError,
  GetInterfaceResponse,
  GetLedgerData,
  GetLedgerError,
  GetLedgerResponse,
  GetNetworkStatsData,
  GetNetworkStatsError,
  GetNetworkStatsResponse,
  GetTransactionData,
  GetTransactionError,
  GetTransactionResponse,
  HealthData,
  HealthResponse2,
  ListAssetsData,
  ListAssetsError,
  ListAssetsResponse,
  ListAssetTransactionsData,
  ListAssetTransactionsError,
  ListAssetTransactionsResponse,
  ListEventsData,
  ListEventsError,
  ListEventsResponse,
  ListInvocationsData,
  ListInvocationsError,
  ListInvocationsResponse,
  ListLedgersData,
  ListLedgersError,
  ListLedgersResponse,
  ListParticipantsData,
  ListParticipantsError,
  ListParticipantsResponse,
  ListTransactionsData,
  ListTransactionsError,
  ListTransactionsResponse,
} from '../types.gen.js';

export type QueryKey<TOptions extends Options> = [
  Pick<TOptions, 'baseUrl' | 'body' | 'headers' | 'path' | 'query'> & {
    _id: string;
    _infinite?: boolean;
    tags?: ReadonlyArray<string>;
  }
];

const createQueryKey = <TOptions extends Options>(
  id: string,
  options?: TOptions,
  infinite?: boolean,
  tags?: ReadonlyArray<string>
): [QueryKey<TOptions>[0]] => {
  const params: QueryKey<TOptions>[0] = {
    _id: id,
    baseUrl:
      options?.baseUrl || (options?.client ?? client).getConfig().baseUrl,
  } as QueryKey<TOptions>[0];
  if (infinite) {
    params._infinite = infinite;
  }
  if (tags) {
    params.tags = tags;
  }
  if (options?.body) {
    params.body = options.body;
  }
  if (options?.headers) {
    params.headers = options.headers;
  }
  if (options?.path) {
    params.path = options.path;
  }
  if (options?.query) {
    params.query = options.query;
  }
  return [params];
};

export const healthQueryKey = (options?: Options<HealthData>) =>
  createQueryKey('health', options);

/**
 * Liveness probe consumed by AWS Lambda health checks and smoke tests.
 */
export const healthOptions = (options?: Options<HealthData>) =>
  queryOptions<
    HealthResponse2,
    DefaultError,
    HealthResponse2,
    ReturnType<typeof healthQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await health({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: healthQueryKey(options),
  });

export const listAssetsQueryKey = (options?: Options<ListAssetsData>) =>
  createQueryKey('listAssets', options);

export const listAssetsOptions = (options?: Options<ListAssetsData>) =>
  queryOptions<
    ListAssetsResponse,
    ListAssetsError,
    ListAssetsResponse,
    ReturnType<typeof listAssetsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await listAssets({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: listAssetsQueryKey(options),
  });

const createInfiniteParams = <
  K extends Pick<QueryKey<Options>[0], 'body' | 'headers' | 'path' | 'query'>
>(
  queryKey: QueryKey<Options>,
  page: K
) => {
  const params = { ...queryKey[0] };
  if (page.body) {
    params.body = {
      ...(queryKey[0].body as any),
      ...(page.body as any),
    };
  }
  if (page.headers) {
    params.headers = {
      ...queryKey[0].headers,
      ...page.headers,
    };
  }
  if (page.path) {
    params.path = {
      ...(queryKey[0].path as any),
      ...(page.path as any),
    };
  }
  if (page.query) {
    params.query = {
      ...(queryKey[0].query as any),
      ...(page.query as any),
    };
  }
  return params as unknown as typeof page;
};

export const listAssetsInfiniteQueryKey = (
  options?: Options<ListAssetsData>
): QueryKey<Options<ListAssetsData>> =>
  createQueryKey('listAssets', options, true);

export const listAssetsInfiniteOptions = (options?: Options<ListAssetsData>) =>
  infiniteQueryOptions<
    ListAssetsResponse,
    ListAssetsError,
    InfiniteData<ListAssetsResponse>,
    QueryKey<Options<ListAssetsData>>,
    | string
    | Pick<
        QueryKey<Options<ListAssetsData>>[0],
        'body' | 'headers' | 'path' | 'query'
      >
  >(
    // @ts-ignore
    {
      queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<
          QueryKey<Options<ListAssetsData>>[0],
          'body' | 'headers' | 'path' | 'query'
        > =
          typeof pageParam === 'object'
            ? pageParam
            : {
                query: {
                  cursor: pageParam,
                },
              };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await listAssets({
          ...options,
          ...params,
          signal,
          throwOnError: true,
        });
        return data;
      },
      queryKey: listAssetsInfiniteQueryKey(options),
    }
  );

export const getAssetQueryKey = (options: Options<GetAssetData>) =>
  createQueryKey('getAsset', options);

export const getAssetOptions = (options: Options<GetAssetData>) =>
  queryOptions<
    GetAssetResponse,
    GetAssetError,
    GetAssetResponse,
    ReturnType<typeof getAssetQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getAsset({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getAssetQueryKey(options),
  });

export const listAssetTransactionsQueryKey = (
  options: Options<ListAssetTransactionsData>
) => createQueryKey('listAssetTransactions', options);

export const listAssetTransactionsOptions = (
  options: Options<ListAssetTransactionsData>
) =>
  queryOptions<
    ListAssetTransactionsResponse,
    ListAssetTransactionsError,
    ListAssetTransactionsResponse,
    ReturnType<typeof listAssetTransactionsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await listAssetTransactions({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: listAssetTransactionsQueryKey(options),
  });

export const listAssetTransactionsInfiniteQueryKey = (
  options: Options<ListAssetTransactionsData>
): QueryKey<Options<ListAssetTransactionsData>> =>
  createQueryKey('listAssetTransactions', options, true);

export const listAssetTransactionsInfiniteOptions = (
  options: Options<ListAssetTransactionsData>
) =>
  infiniteQueryOptions<
    ListAssetTransactionsResponse,
    ListAssetTransactionsError,
    InfiniteData<ListAssetTransactionsResponse>,
    QueryKey<Options<ListAssetTransactionsData>>,
    | string
    | Pick<
        QueryKey<Options<ListAssetTransactionsData>>[0],
        'body' | 'headers' | 'path' | 'query'
      >
  >(
    // @ts-ignore
    {
      queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<
          QueryKey<Options<ListAssetTransactionsData>>[0],
          'body' | 'headers' | 'path' | 'query'
        > =
          typeof pageParam === 'object'
            ? pageParam
            : {
                query: {
                  cursor: pageParam,
                },
              };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await listAssetTransactions({
          ...options,
          ...params,
          signal,
          throwOnError: true,
        });
        return data;
      },
      queryKey: listAssetTransactionsInfiniteQueryKey(options),
    }
  );

export const getContractQueryKey = (options: Options<GetContractData>) =>
  createQueryKey('getContract', options);

export const getContractOptions = (options: Options<GetContractData>) =>
  queryOptions<
    GetContractResponse,
    GetContractError,
    GetContractResponse,
    ReturnType<typeof getContractQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getContract({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getContractQueryKey(options),
  });

export const listEventsQueryKey = (options: Options<ListEventsData>) =>
  createQueryKey('listEvents', options);

export const listEventsOptions = (options: Options<ListEventsData>) =>
  queryOptions<
    ListEventsResponse,
    ListEventsError,
    ListEventsResponse,
    ReturnType<typeof listEventsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await listEvents({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: listEventsQueryKey(options),
  });

export const listEventsInfiniteQueryKey = (
  options: Options<ListEventsData>
): QueryKey<Options<ListEventsData>> =>
  createQueryKey('listEvents', options, true);

export const listEventsInfiniteOptions = (options: Options<ListEventsData>) =>
  infiniteQueryOptions<
    ListEventsResponse,
    ListEventsError,
    InfiniteData<ListEventsResponse>,
    QueryKey<Options<ListEventsData>>,
    | string
    | Pick<
        QueryKey<Options<ListEventsData>>[0],
        'body' | 'headers' | 'path' | 'query'
      >
  >(
    // @ts-ignore
    {
      queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<
          QueryKey<Options<ListEventsData>>[0],
          'body' | 'headers' | 'path' | 'query'
        > =
          typeof pageParam === 'object'
            ? pageParam
            : {
                query: {
                  cursor: pageParam,
                },
              };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await listEvents({
          ...options,
          ...params,
          signal,
          throwOnError: true,
        });
        return data;
      },
      queryKey: listEventsInfiniteQueryKey(options),
    }
  );

export const getInterfaceQueryKey = (options: Options<GetInterfaceData>) =>
  createQueryKey('getInterface', options);

export const getInterfaceOptions = (options: Options<GetInterfaceData>) =>
  queryOptions<
    GetInterfaceResponse,
    GetInterfaceError,
    GetInterfaceResponse,
    ReturnType<typeof getInterfaceQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getInterface({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getInterfaceQueryKey(options),
  });

export const listInvocationsQueryKey = (
  options: Options<ListInvocationsData>
) => createQueryKey('listInvocations', options);

export const listInvocationsOptions = (options: Options<ListInvocationsData>) =>
  queryOptions<
    ListInvocationsResponse,
    ListInvocationsError,
    ListInvocationsResponse,
    ReturnType<typeof listInvocationsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await listInvocations({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: listInvocationsQueryKey(options),
  });

export const listInvocationsInfiniteQueryKey = (
  options: Options<ListInvocationsData>
): QueryKey<Options<ListInvocationsData>> =>
  createQueryKey('listInvocations', options, true);

export const listInvocationsInfiniteOptions = (
  options: Options<ListInvocationsData>
) =>
  infiniteQueryOptions<
    ListInvocationsResponse,
    ListInvocationsError,
    InfiniteData<ListInvocationsResponse>,
    QueryKey<Options<ListInvocationsData>>,
    | string
    | Pick<
        QueryKey<Options<ListInvocationsData>>[0],
        'body' | 'headers' | 'path' | 'query'
      >
  >(
    // @ts-ignore
    {
      queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<
          QueryKey<Options<ListInvocationsData>>[0],
          'body' | 'headers' | 'path' | 'query'
        > =
          typeof pageParam === 'object'
            ? pageParam
            : {
                query: {
                  cursor: pageParam,
                },
              };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await listInvocations({
          ...options,
          ...params,
          signal,
          throwOnError: true,
        });
        return data;
      },
      queryKey: listInvocationsInfiniteQueryKey(options),
    }
  );

export const listLedgersQueryKey = (options?: Options<ListLedgersData>) =>
  createQueryKey('listLedgers', options);

/**
 * List ledgers ordered by `(closed_at DESC, sequence DESC)` with cursor
 * pagination.
 */
export const listLedgersOptions = (options?: Options<ListLedgersData>) =>
  queryOptions<
    ListLedgersResponse,
    ListLedgersError,
    ListLedgersResponse,
    ReturnType<typeof listLedgersQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await listLedgers({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: listLedgersQueryKey(options),
  });

export const listLedgersInfiniteQueryKey = (
  options?: Options<ListLedgersData>
): QueryKey<Options<ListLedgersData>> =>
  createQueryKey('listLedgers', options, true);

/**
 * List ledgers ordered by `(closed_at DESC, sequence DESC)` with cursor
 * pagination.
 */
export const listLedgersInfiniteOptions = (
  options?: Options<ListLedgersData>
) =>
  infiniteQueryOptions<
    ListLedgersResponse,
    ListLedgersError,
    InfiniteData<ListLedgersResponse>,
    QueryKey<Options<ListLedgersData>>,
    | string
    | Pick<
        QueryKey<Options<ListLedgersData>>[0],
        'body' | 'headers' | 'path' | 'query'
      >
  >(
    // @ts-ignore
    {
      queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<
          QueryKey<Options<ListLedgersData>>[0],
          'body' | 'headers' | 'path' | 'query'
        > =
          typeof pageParam === 'object'
            ? pageParam
            : {
                query: {
                  cursor: pageParam,
                },
              };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await listLedgers({
          ...options,
          ...params,
          signal,
          throwOnError: true,
        });
        return data;
      },
      queryKey: listLedgersInfiniteQueryKey(options),
    }
  );

export const getLedgerQueryKey = (options: Options<GetLedgerData>) =>
  createQueryKey('getLedger', options);

/**
 * Get ledger detail by sequence — header + prev/next navigation +
 * embedded paginated transactions.
 *
 * Two phases, both DB-only:
 *
 * 1. **DB header.** Resolve `:sequence` against `ledgers` + LATERAL
 * prev/next computed via `sequence` comparisons on the `ledgers`
 * PK (index-only scan, no heap fetch). 404 on miss.
 * 2. **DB transactions.** Keyset-paginated read of the `transactions`
 * partition with full equality partition prune
 * (`created_at = $closed_at`).
 *
 * The detail endpoint reuses the standard `?limit=` / `?cursor=` query
 * parameters to drive embedded transactions pagination. Detail itself
 * is a single resource, so no naming collision arises — the params
 * page the embedded list directly.
 */
export const getLedgerOptions = (options: Options<GetLedgerData>) =>
  queryOptions<
    GetLedgerResponse,
    GetLedgerError,
    GetLedgerResponse,
    ReturnType<typeof getLedgerQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getLedger({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getLedgerQueryKey(options),
  });

export const getLedgerInfiniteQueryKey = (
  options: Options<GetLedgerData>
): QueryKey<Options<GetLedgerData>> =>
  createQueryKey('getLedger', options, true);

/**
 * Get ledger detail by sequence — header + prev/next navigation +
 * embedded paginated transactions.
 *
 * Two phases, both DB-only:
 *
 * 1. **DB header.** Resolve `:sequence` against `ledgers` + LATERAL
 * prev/next computed via `sequence` comparisons on the `ledgers`
 * PK (index-only scan, no heap fetch). 404 on miss.
 * 2. **DB transactions.** Keyset-paginated read of the `transactions`
 * partition with full equality partition prune
 * (`created_at = $closed_at`).
 *
 * The detail endpoint reuses the standard `?limit=` / `?cursor=` query
 * parameters to drive embedded transactions pagination. Detail itself
 * is a single resource, so no naming collision arises — the params
 * page the embedded list directly.
 */
export const getLedgerInfiniteOptions = (options: Options<GetLedgerData>) =>
  infiniteQueryOptions<
    GetLedgerResponse,
    GetLedgerError,
    InfiniteData<GetLedgerResponse>,
    QueryKey<Options<GetLedgerData>>,
    | string
    | Pick<
        QueryKey<Options<GetLedgerData>>[0],
        'body' | 'headers' | 'path' | 'query'
      >
  >(
    // @ts-ignore
    {
      queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<
          QueryKey<Options<GetLedgerData>>[0],
          'body' | 'headers' | 'path' | 'query'
        > =
          typeof pageParam === 'object'
            ? pageParam
            : {
                query: {
                  cursor: pageParam,
                },
              };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await getLedger({
          ...options,
          ...params,
          signal,
          throwOnError: true,
        });
        return data;
      },
      queryKey: getLedgerInfiniteQueryKey(options),
    }
  );

export const listParticipantsQueryKey = (
  options: Options<ListParticipantsData>
) => createQueryKey('listParticipants', options);

export const listParticipantsOptions = (
  options: Options<ListParticipantsData>
) =>
  queryOptions<
    ListParticipantsResponse,
    ListParticipantsError,
    ListParticipantsResponse,
    ReturnType<typeof listParticipantsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await listParticipants({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: listParticipantsQueryKey(options),
  });

export const listParticipantsInfiniteQueryKey = (
  options: Options<ListParticipantsData>
): QueryKey<Options<ListParticipantsData>> =>
  createQueryKey('listParticipants', options, true);

export const listParticipantsInfiniteOptions = (
  options: Options<ListParticipantsData>
) =>
  infiniteQueryOptions<
    ListParticipantsResponse,
    ListParticipantsError,
    InfiniteData<ListParticipantsResponse>,
    QueryKey<Options<ListParticipantsData>>,
    | string
    | Pick<
        QueryKey<Options<ListParticipantsData>>[0],
        'body' | 'headers' | 'path' | 'query'
      >
  >(
    // @ts-ignore
    {
      queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<
          QueryKey<Options<ListParticipantsData>>[0],
          'body' | 'headers' | 'path' | 'query'
        > =
          typeof pageParam === 'object'
            ? pageParam
            : {
                query: {
                  cursor: pageParam,
                },
              };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await listParticipants({
          ...options,
          ...params,
          signal,
          throwOnError: true,
        });
        return data;
      },
      queryKey: listParticipantsInfiniteQueryKey(options),
    }
  );

export const getNetworkStatsQueryKey = (
  options?: Options<GetNetworkStatsData>
) => createQueryKey('getNetworkStats', options);

/**
 * Get top-level chain overview stats.
 *
 * Reads the canonical single-statement network-stats query (latest
 * ledger row + `ledgers` 60s aggregate for TPS + `pg_class.reltuples`
 * estimates for accounts / contracts) and caches the assembled
 * response for 30s in process memory. See the task 0045 spec and
 * `docs/architecture/database-schema/endpoint-queries/01_get_network_stats.sql`
 * for the full data-source mapping.
 */
export const getNetworkStatsOptions = (
  options?: Options<GetNetworkStatsData>
) =>
  queryOptions<
    GetNetworkStatsResponse,
    GetNetworkStatsError,
    GetNetworkStatsResponse,
    ReturnType<typeof getNetworkStatsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getNetworkStats({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getNetworkStatsQueryKey(options),
  });

export const listTransactionsQueryKey = (
  options?: Options<ListTransactionsData>
) => createQueryKey('listTransactions', options);

/**
 * List transactions with optional filters and cursor-based pagination.
 */
export const listTransactionsOptions = (
  options?: Options<ListTransactionsData>
) =>
  queryOptions<
    ListTransactionsResponse,
    ListTransactionsError,
    ListTransactionsResponse,
    ReturnType<typeof listTransactionsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await listTransactions({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: listTransactionsQueryKey(options),
  });

export const listTransactionsInfiniteQueryKey = (
  options?: Options<ListTransactionsData>
): QueryKey<Options<ListTransactionsData>> =>
  createQueryKey('listTransactions', options, true);

/**
 * List transactions with optional filters and cursor-based pagination.
 */
export const listTransactionsInfiniteOptions = (
  options?: Options<ListTransactionsData>
) =>
  infiniteQueryOptions<
    ListTransactionsResponse,
    ListTransactionsError,
    InfiniteData<ListTransactionsResponse>,
    QueryKey<Options<ListTransactionsData>>,
    | string
    | Pick<
        QueryKey<Options<ListTransactionsData>>[0],
        'body' | 'headers' | 'path' | 'query'
      >
  >(
    // @ts-ignore
    {
      queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<
          QueryKey<Options<ListTransactionsData>>[0],
          'body' | 'headers' | 'path' | 'query'
        > =
          typeof pageParam === 'object'
            ? pageParam
            : {
                query: {
                  cursor: pageParam,
                },
              };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await listTransactions({
          ...options,
          ...params,
          signal,
          throwOnError: true,
        });
        return data;
      },
      queryKey: listTransactionsInfiniteQueryKey(options),
    }
  );

export const getTransactionQueryKey = (options: Options<GetTransactionData>) =>
  createQueryKey('getTransaction', options);

/**
 * Get a single transaction by hash.
 *
 * Returns the wrapped E3 response from task 0150: the DB-sourced
 * `TransactionDetailLight` (flattened to the top level) plus a `heavy` block
 * carrying every XDR-sourced field — memo, result_code, signatures, fee-bump
 * source, envelope/result XDR, contract + diagnostic events, per-operation
 * decoded details, and the nested `operation_tree`. `heavy_fields_status` is
 * `"ok"` when the public-archive fetch succeeded and `"unavailable"` when it
 * failed (graceful degradation per ADR 0029 — the light slice is always
 * returned). Per ADR 0033 there is no separate "advanced" view; the wrapper
 * always carries the full heavy payload when available.
 */
export const getTransactionOptions = (options: Options<GetTransactionData>) =>
  queryOptions<
    GetTransactionResponse,
    GetTransactionError,
    GetTransactionResponse,
    ReturnType<typeof getTransactionQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getTransaction({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getTransactionQueryKey(options),
  });
